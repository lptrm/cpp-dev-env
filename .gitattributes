# Set the default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files to prevent any CRLF issues
*.cpp text
*.h text
*.hpp text
*.c text
*.txt text
README text
LICENSE text

# Declare files that will always have CRLF line endings on checkout.
*.bat text eol=crlf

# Declare files that will always have LF line endings on checkout.
*.sh text eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.bmp binary
*.tiff binary
*.tif binary
*.pdf binary
*.zip binary
*.rar binary
*.exe binary
*.dll binary
*.so binary
*.dylib binary
*.obj binary
*.o binary
*.a binary
*.lib binary
*.pdb binary
*.dmp binary

# Diff settings
*.bat diff=astextplain
*.sh diff=astextplain
*.html diff=html
*.css diff=css
*.js diff=js
*.json diff=json
*.xml diff=xml
*.yml diff=yaml
*.yaml diff=yaml

# Custom for C++
*.cpp diff=cpp
*.h diff=cpp
*.hpp diff=cpp

# Denote all files that are truly binary and should not be modified.
#
# NOTE: When using custom tools that generate specific files, you might
# want to mark these files as binary to prevent Git from attempting to
# auto-convert or perform line normalization on them.

# Linguist settings
# Override GitHub Linguist's language detection
# Example: Treat .h files as C++ (instead of C by default)
*.h linguist-language=C++
